/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@client";
import type {
	PostTransactionsMutationRequest,
	PostTransactionsMutationResponse,
	PostTransactions400,
	PostTransactions401,
	PostTransactions403,
	PostTransactions404,
} from "../types/PostTransactions.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@client";

function getPostTransactionsUrl() {
	const res = { method: "POST", url: `/transactions` as const };
	return res;
}

/**
 * @description Cria uma nova transação no workspace. Se bucket_id não for informado, a transação é automaticamente atribuída ao Caixa de Entrada (INBOX).
 * @summary Criar transação
 * {@link /transactions}
 */
export async function postTransactions(
	data: PostTransactionsMutationRequest,
	config: Partial<RequestConfig<PostTransactionsMutationRequest>> & {
		client?: Client;
	} = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const requestData = data;

	const res = await request<
		PostTransactionsMutationResponse,
		ResponseErrorConfig<
			| PostTransactions400
			| PostTransactions401
			| PostTransactions403
			| PostTransactions404
		>,
		PostTransactionsMutationRequest
	>({
		method: "POST",
		url: getPostTransactionsUrl().url.toString(),
		data: requestData,
		...requestConfig,
	});
	return res.data;
}
