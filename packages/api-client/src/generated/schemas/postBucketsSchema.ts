/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "zod/v4";

/**
 * @description Default Response
 */
export const postBuckets201Schema = z.object({
	id: z.string().describe("ID do caixa"),
	workspace_id: z.string().describe("ID do workspace"),
	name: z.string().describe("Nome do caixa"),
	type: z.enum(["SPENDING", "INVESTMENT", "INBOX"]).describe("Tipo do caixa"),
	allocation_percentage: z.number().describe("Percentual de alocação (0-100)"),
	is_default: z.boolean().describe("Indica se é o caixa padrão (INBOX)"),
	created_at: z.iso.datetime().describe("Data de criação"),
});

/**
 * @description Erro de validação ou header ausente
 */
export const postBuckets400Schema = z
	.object({
		code: z.enum([
			"NOT_FOUND",
			"UNAUTHORIZED",
			"FORBIDDEN",
			"BAD_REQUEST",
			"CONFLICT",
			"INTERNAL_SERVER_ERROR",
			"VALIDATION_ERROR",
			"HEALTH_CHECK_FAILED",
			"TOO_MANY_REQUESTS",
		]),
		message: z.string(),
		details: z.optional(z.object({}).catchall(z.any())),
	})
	.describe("Erro de validação ou header ausente");

/**
 * @description Token inválido ou ausente
 */
export const postBuckets401Schema = z
	.object({
		code: z.enum([
			"NOT_FOUND",
			"UNAUTHORIZED",
			"FORBIDDEN",
			"BAD_REQUEST",
			"CONFLICT",
			"INTERNAL_SERVER_ERROR",
			"VALIDATION_ERROR",
			"HEALTH_CHECK_FAILED",
			"TOO_MANY_REQUESTS",
		]),
		message: z.string(),
		details: z.optional(z.object({}).catchall(z.any())),
	})
	.describe("Token inválido ou ausente");

/**
 * @description Sem permissão no workspace
 */
export const postBuckets403Schema = z
	.object({
		code: z.enum([
			"NOT_FOUND",
			"UNAUTHORIZED",
			"FORBIDDEN",
			"BAD_REQUEST",
			"CONFLICT",
			"INTERNAL_SERVER_ERROR",
			"VALIDATION_ERROR",
			"HEALTH_CHECK_FAILED",
			"TOO_MANY_REQUESTS",
		]),
		message: z.string(),
		details: z.optional(z.object({}).catchall(z.any())),
	})
	.describe("Sem permissão no workspace");

export const postBucketsMutationRequestSchema = z.object({
	name: z.string().min(1).max(100).describe("Nome do caixa"),
	type: z.optional(
		z
			.enum(["SPENDING", "INVESTMENT"])
			.default("SPENDING")
			.describe("Tipo do caixa"),
	),
	allocation_percentage: z.optional(
		z
			.number()
			.min(0)
			.max(100)
			.default(0)
			.describe("Percentual de alocação (0-100)"),
	),
});

export const postBucketsMutationResponseSchema = z.lazy(
	() => postBuckets201Schema,
);
