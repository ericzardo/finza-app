info:
  name: create-user
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:9999/users
  body:
    type: json
    data: |-
      {
        "name": "Eric Zardo",
        "email": "eric@zardo.dev",
        "password": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
