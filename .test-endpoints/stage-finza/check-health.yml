info:
  name: check-health
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:9999/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
